import { HttpClientImpl, FetchOptions } from 'sp-pnp-js';
import { IPnpNodeSettings } from './interfaces';
export declare class PnpNode implements HttpClientImpl {
    private settings;
    private spAuthConfigirator;
    private agent;
    private utils;
    constructor(settings?: IPnpNodeSettings);
    fetch: (url: string, options: FetchOptions) => Promise<any>;
    init: () => Promise<IPnpNodeSettings>;
    initAmbient: () => Promise<IPnpNodeSettings>;
}
