import { Headers } from 'node-fetch';
export declare class Utils {
    checkNestedProperties(object: any, ...args: string[]): boolean;
    getCaseInsensitiveProp(object: any, propertyName: string): any;
    isOnPrem(url: string): boolean;
    isUrlHttps(url: string): boolean;
    isUrlAbsolute(url: string): boolean;
    combineUrl(...args: string[]): string;
    mergeHeaders(...args: any[]): Headers;
    anyToHeaders(headers?: any): Headers;
}
