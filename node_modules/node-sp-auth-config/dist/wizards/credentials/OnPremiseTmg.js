"use strict";
var __assign = (this && this.__assign) || Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
        s = arguments[i];
        for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
            t[p] = s[p];
    }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
var inquirer = require("inquirer");
var utils_1 = require("../../utils");
var wizard = function (authContext, answersAll, settings) {
    if (answersAll === void 0) { answersAll = {}; }
    if (settings === void 0) { settings = {}; }
    return new Promise(function (resolve, reject) {
        var onPremiseTmgCredentials = authContext.authOptions;
        var promptFor = [
            {
                name: 'username',
                message: 'User name',
                type: 'input',
                default: onPremiseTmgCredentials.username,
                validate: function (answer) {
                    if (answer.length === 0) {
                        return false;
                    }
                    return true;
                }
            }, {
                name: 'password',
                message: 'Password',
                type: 'password',
                default: onPremiseTmgCredentials.password ? utils_1.defaultPasswordMask : null,
                validate: function (answer) {
                    if (answer.length === 0) {
                        return false;
                    }
                    return true;
                }
            }
        ];
        inquirer.prompt(promptFor)
            .then(function (answers) {
            answersAll = __assign({}, answersAll, answers, { password: answers.password === utils_1.defaultPasswordMask
                    ? onPremiseTmgCredentials.password
                    : answers.password }, {
                tmg: true
            });
            resolve(answersAll);
        });
    });
};
exports.default = wizard;

//# sourceMappingURL=OnPremiseTmg.js.map
